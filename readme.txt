
1. Open the project in IntelliJ, need to add apache.lucene, jackson, Jsoup  .jar file (not included)
2. run main() with IntelliJ
3. Json ads saved in objectMap.json

4. imlemented paging

5. problems remained:
5.1 certain result elements of a few queries are null, I skipped them
5.2 a few of the query results returned http 400, which are not showed in the results